export function NivelesPage() {
  return (
    <div>
      {/* === HERO DE SECCIÓN === */}
      {/* === PRE-KÍNDER / KÍNDER === */}
      {/* === BÁSICA (1° - 8°) === */}
      {/* === MEDIA (1° - 4° MEDIO) === */}
      {/* === EXTRAESCOLARES === */}
    </div>
  )
}
