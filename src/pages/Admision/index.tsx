export function AdmisionPage() {
  return (
    <div>
      {/* === PASOS DEL PROCESO === */}
      {/* === REQUISITOS === */}
      {/* === CALENDARIO DE ADMISIÓN === */}
      {/* === FORMULARIO DE PRE-INSCRIPCIÓN === */}
      {/* === FAQ ADMISIÓN === */}
    </div>
  )
}
