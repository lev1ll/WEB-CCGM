export function NosotrosPage() {
  return (
    <div>
      {/* === HISTORIA === */}
      {/* === MISIÓN / VISIÓN (Bento Grid) === */}
      {/* === EQUIPO DIRECTIVO === */}
      {/* === VALORES INSTITUCIONALES === */}
    </div>
  )
}
