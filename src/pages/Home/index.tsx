export function HomePage() {
  return (
    <div>
      {/* === HERO === */}
      {/* === SOBRE NOSOTROS (resumen) === */}
      {/* === NIVELES EDUCATIVOS === */}
      {/* === VALORES === */}
      {/* === TESTIMONIOS === */}
      {/* === GALERÍA === */}
      {/* === FAQ === */}
      {/* === CTA ADMISIÓN === */}
    </div>
  )
}
